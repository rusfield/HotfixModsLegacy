<MudBadge Origin="Origin.CenterRight" Overlap="true" Icon="@Icons.Filled.ArrowBack" Style="width:100%" Class="@(CssClass + " invisible")">
    <MudTextField T="string" Value="@("Placeholder")"  Label="@("Placeholder")" Variant="@Variant" />
</MudBadge>


@code
{
    [Parameter]
    public string CssClass { get; set; }

    [Parameter]
    public Variant Variant { get; set; } = Variant.Outlined;
}
