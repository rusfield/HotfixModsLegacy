@using HotfixMods.Infrastructure.Blazor.Components.DtoContent
@using HotfixMods.Core.Flags.Db2

@inherits DtoContentBase<AnimKitDto, AnimKitConfig>

<InstancedDtoContentPage>
    <MudItem xs="3">
        <Input_Element T="int" @bind-Value="@(Value.ConfigFlags)" Label="@(nameof(Value.ConfigFlags).ToDisplayName(null, " (ms)"))" ValueCompare="ValueCompare?.ConfigFlags" OnInfoAdornmentClick="@(OpenInfoDialog(nameof(AnimKitConfig.ConfigFlags)))" GetOptionsAsync_Func="GetConfigFlags" MultiSelection="true" />
    </MudItem>
    <MudItem xs="9" />
</InstancedDtoContentPage>


@code {

    async Task<Dictionary<int, string>> GetConfigFlags()
    {
        return Enum.GetValues<AnimKitConfigConfigFlags>().ToDictionary(key => (int)key, value => value.ToDisplayString());
    }
}
