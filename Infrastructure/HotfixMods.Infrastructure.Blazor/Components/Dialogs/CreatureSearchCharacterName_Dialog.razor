@using HotfixMods.Infrastructure.Blazor.Components.Private

@inject CreatureService Service

<DialogSearch @ref="dialogSearchRef" MudDialogInstance="Dialog">
    <MudText Typo="Typo.h6" Align="Align.Center">Search by Character Name:</MudText>
    <MudTextField @bind-Value="@CharacterName" Variant="Variant.Outlined" Label="Character Name" Adornment="Adornment.End" AdornmentIcon="@Icons.Material.Filled.Search" OnAdornmentClick="(GetByCharacterNameAsync)" />
</DialogSearch>

@code {
    [CascadingParameter]
    MudDialogInstance Dialog { get; set; }

    [Parameter]
    public string? CharacterName { get; set; }

    DialogSearch? dialogSearchRef;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await GetByCharacterNameAsync();
        }
        await base.OnAfterRenderAsync(firstRender);
    }

    async Task GetByCharacterNameAsync()
    {
        if (!string.IsNullOrWhiteSpace(CharacterName))
        {
            var data = await Service.GetByCharacterNameAsync(CharacterName, dialogSearchRef!.SetLoaderProgress);
            if (data != null)
            {
                Dialog.Close(data);
            }
            else
            {
                dialogSearchRef.CancelLoaderWithMessage("No DB2 record found.");
            }
        }
    }
}
