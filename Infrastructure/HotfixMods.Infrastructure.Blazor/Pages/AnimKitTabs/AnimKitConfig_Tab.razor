@using HotfixMods.Infrastructure.InfoModels
@using HotfixMods.Infrastructure.Blazor.Components.DtoContent
@inherits DtoContentBase<AnimKitDto, AnimKitConfig>

<MudItem xs="3">
    <Input_Element T="uint" @bind-Value="@(Value.ConfigFlags)" Label="@(nameof(Value.ConfigFlags).ToDisplayName(null, " (ms)"))" ValueCompare="ValueCompare?.ConfigFlags" />
</MudItem>
<MudItem xs="9" />

@code{
    protected override void OnParametersSet()
    {
        base.OnParametersSet();
    }
}