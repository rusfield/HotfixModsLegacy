@using HotfixMods.Core.Enums.Db2;
@using HotfixMods.Infrastructure.InfoModels
@using HotfixMods.Infrastructure.Blazor.Components.DtoContent

@inherits DtoContentBase<SoundKitDto, SoundKitEntry>

<InstancedDtoContentPage>
    <MudItem xs="3">
        <Input_Element T="int" @bind-Value="@(Value.FileDataId)" Label="@(nameof(Value.FileDataId).ToDisplayName())" ValueCompare="ValueCompare?.FileDataId" />
        <Input_Element T="byte" @bind-Value="@(Value.Frequency)" Label="@(nameof(Value.Frequency).ToDisplayName())" ValueCompare="ValueCompare?.Frequency" />
        <Input_Element T="decimal" @bind-Value="@(Value.Volume)" Label="@(nameof(Value.Volume).ToDisplayName())" ValueCompare="ValueCompare?.Volume" />
    </MudItem>
    <MudItem xs="9" />
</InstancedDtoContentPage>

@code {
    SoundKitEntryInfo info = new();
}