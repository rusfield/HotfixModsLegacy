@using HotfixMods.Infrastructure.Blazor.Components.Dialogs;
@using HotfixMods.Infrastructure.Blazor.Components.DtoContent

@inject SoundKitService Service
@inherits PageBase<SoundKitDto>

<DtoTabsContent T="SoundKitDto" SaveAsync_Func="Service.SaveAsync" ValueCompare_Callback="@(async () => ValueCompareAsync<SoundKitSearchId_Dialog>())" RequiredTypes="required" />


@code {
    List<Type> required = new()
    {
        typeof(SoundKit)
    };
}
