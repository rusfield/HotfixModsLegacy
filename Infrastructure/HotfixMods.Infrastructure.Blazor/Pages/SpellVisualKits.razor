@using HotfixMods.Infrastructure.Blazor.Components.Dialogs;
@using HotfixMods.Infrastructure.Blazor.Components.DtoContent

@inject SpellVisualKitService Service
@inherits PageBase<SpellVisualKitDto>

<DtoTabsContent T="SpellVisualKitDto" SaveAsync_Func="Service.SaveAsync" ValueCompare_Callback="@(async () => ValueCompareAsync<SpellVisualKitSearchId_Dialog>())" />


@code {

    // Temp
    protected override void OnParametersSet()
    {
        if (null == PageTab.Dto)
            PageTab.Dto = new SpellVisualKitDto();
        base.OnParametersSet();
    }
}
