@inherits PageBase<AnimKitDto>

@using HotfixMods.Infrastructure.Blazor.Pages.AnimKitTabs
@using HotfixMods.Infrastructure.Blazor.Components.DtoContent

@* IDEA FOR MIDDLE IMAGE DISPLAY
    Stack a bunch of images on top of each other. One image for each body part + children. Use Z-index to order images, so that you can verify that the priorities are right.

*@

<DtoTabsContent T="AnimKitDto" />


@code {

    // Temp
    protected override void OnParametersSet()
    {
        if(PageTab.Dto == null)
            PageTab.Dto = new AnimKitDto();
        base.OnParametersSet();
    }


}
